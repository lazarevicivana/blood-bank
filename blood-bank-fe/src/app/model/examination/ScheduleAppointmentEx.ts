export class ScheduleAppointmentEx {
  centerId?: string;
  customerId?: string;

  constructor(centerId?: string, customerId?: string) {
    this.centerId = centerId;
    this.customerId = customerId;
  }

}
